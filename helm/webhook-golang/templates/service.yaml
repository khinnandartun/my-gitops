apiVersion: v1
kind: Service
metadata:
  name: {{ include "webhook-golang.fullname" . }}
  labels:
    {{- include "webhook-golang.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webhook-golang.selectorLabels" . | nindent 4 }}
